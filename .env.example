# Copy to .env and set before `docker compose up`.
AXIOM_API_KEY=your-production-api-key-here
AXIOM_REQUIRE_API_KEY=1
AXIOM_BUNDLE_SECRET=
# Local dev only (never in production):
# AXIOM_ALLOW_INSECURE_SERVE=1
