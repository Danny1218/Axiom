// Sequence crucible: while loop drives InterpretedLiquidLoop + LiquidKANNode (RBF splines).
// Trunk supplies angle x; loop integrates step; y_pred is the supervised output (fit to sin(x)).

x = 0.0;
step = 0.0;
while (step < 10.0) {
  step = step + 1.0;
}
y_pred = 0.0;
