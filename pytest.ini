[pytest]
markers =
    slow: benchmarks, training loops, or subprocess profiling (>5s typical)
    compile: torch.compile / fullgraph tracing tests
    integration: network or external API (e.g. yfinance)
